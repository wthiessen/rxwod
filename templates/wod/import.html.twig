{% extends 'base.html.twig' %}

{% block body %}
    <div class="container" ng-app="myApp" ng-controller="rxWodImportCtrl" data-token="{{ secret }}" data-glofox-url="{{ glofox_url }}">
        <div class="row">
            <h2>
                <span class="add-wod-form-2">Add Wods</span>
            </h2>

            <br>
            <div class="form-group add-wod">
                <form action="{{ path('app_wod_import') }}" method="POST">
                    <div><label for="date">Date</label> <input class="createdAt" type="date" name="createdAt" id="createdAt" value="{{ "now"|date("Y-m-d") }}" class="form-control" required></div><br>
                    {:created:}<br><br>
                    <div><label for="wod">Wod </label> <textarea style="width: 66%; height: 500px; display: block; resize: vertical;" name="wod" required>{:content:}</textarea></div>
                    <br>
                    <div><button type="submit">Submit</button>
                </form>
            </div>
        </div>
    </div>
{% endblock %}

